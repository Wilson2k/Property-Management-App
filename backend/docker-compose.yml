volumes:
  data:

services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_PASSWORD=postgrespw
      - POSTGRES_DB=testdb
    ports:
      - 5432:5432
    volumes:
      - data:/var/lib/postgresql
